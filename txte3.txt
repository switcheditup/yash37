all prac commands

For harvester
go to Linux kali in the command line
type
theHarverster 
theHarvester -d
the Harvester -d example.com -l 50 -b all

FOR lynis
sud apt install
sudo lynis audit (its wrong command you know but still)
lynis audit system (correct command)

For Nikto
sudo docker pull vulnerables/web-dvwa
sudo docker run -d -p 8080:80 vulnerables/web-dvwa
(or run this command)
sudo docker run -d -p 8080 vulnerables/web-dvwa
create new tab
there 
http//localhost:8080/setup.php
your codes is done


MIMT attack using Wireshark
go to the settings in Linux (you can get there using machine in topleft)
the go to networks go to adaptor 1 -> from NAT->bridge adaptor->in Promicuous turn it to allow all->click allow all
Go to wireshark you see option as eth0 right click at start capture
you will get there in search icon press the green button(traffic) (keep it runnnin until u login into vulnweb.com
to get to http://testasp.vulnweb.com/ (in windows it is remember)
in login put user/admin name and password (after entering you get as invalid means it runs) 
now stop the green button (traffic)
u will see post/login asp? click that
after that pop up scroll the  horizontal slider bar towards the left and vertical scroll bar down you will see your login info and password
your work is done



Veracrypt encryption 
in windows open it also open it in Linux too
in windows create folder put txt file inside it 
select file size 1mb
in VeraCrypt create volume ->select the file->create password 
then move mouse side ti side until it turns green and format it
after ull get volume created
Now go the Linux->go to seetings ->shared folder ->remove any existing path-> add path ->select folder->auto mout->ok
you will find the file/folder in Linux
work done


Better cap
sudo bettercap(u will see something like this >>)
help
net.show
net.probe on


Change “Attached to” setting of VM to Bridged Adapter
In kali terminal: sudo bettercap
Help
net.probe on
help
events.stream off
set net.sniff verbose true
svents.stream on
net.sniff on
Open browser in windows>vulnweb> 2nd link> signup> username and password : test
Back to kali
ctrl +shift+f
Enter “test”
Net.sniff off

Juice shop

sudo docker pull bkimminich/juice-shop
sudo docker run --rm -p 127.0.0.1:3000:3000 bkimminich/juice-shop
http://localhost:3000








